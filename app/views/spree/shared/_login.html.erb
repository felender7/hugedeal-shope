
<%= form_for Spree::User.new, :as => :spree_user, :url => spree.create_new_session_path do |f| %>
  <div id="password-credentials">
    <div class='form-group'>
      <%= f.email_field :email, :class => 'title', :tabindex => 1, autofocus: true, class: 'input', placeholder:"Email" %>
    </div>
    <div class='form-group'>
      <%= f.password_field :password, :class => 'title', :tabindex => 2, class: 'input', placeholder:"Password" %>
    </div>
  </div>
  <div class='form-group'>
    <%= f.check_box :remember_me, :tabindex => 3 %>
    <%= f.label :remember_me, Spree.t(:remember_me) %>
  </div>

  <div class='actions'>
    <%= f.submit Spree.t(:login), :class => ' btn success-btn btn-block', :tabindex => 4 %>
  </div>
<% end %>
